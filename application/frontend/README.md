# TODO App - Frontend

Next.js 15を使用したフロントエンドアプリケーション

## 機能

- **開発時のみ表示されるボタン**: ヘッダーに開発環境でのみ表示される特別なボタンがあり、特定のAPIエンドポイントを呼び出すことができます
- **レスポンシブデザイン**: Tailwind CSSによるモダンなデザイン
- **TypeScript**: 型安全性を確保

## 開発環境のセットアップ

### 依存関係のインストール

```bash
npm install
```

### 環境変数の設定

`.env.local`ファイルを作成し、以下の環境変数を設定：

```env
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000
```

### 開発サーバーの起動

```bash
npm run dev
```

ブラウザで [http://localhost:3000](http://localhost:3000) を開きます。

## 開発用ボタンについて

ヘッダーに表示される「🔧 Dev: API呼び出し」ボタンは、`NODE_ENV=development`の時のみ表示されます。

### カスタマイズ方法

`components/header.tsx`の以下の部分を編集：

```typescript
const handleDevButtonClick = async () => {
  // ここでエンドポイントを変更
  const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/api/debug`, {
    method: 'GET', // または 'POST', 'PUT', etc.
    headers: {
      'Content-Type': 'application/json',
    },
    // POSTの場合
    // body: JSON.stringify({ data: 'value' }),
  });
  // ...
};
```

### 呼び出すエンドポイントの変更

1. `components/header.tsx`を開く
2. `handleDevButtonClick`関数内のfetch URLを変更
3. 必要に応じてHTTPメソッドやリクエストボディを変更

## 本番ビルド

```bash
npm run build
npm start
```

本番環境では開発用ボタンは表示されません。

## ディレクトリ構造

```
frontend/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # ルートレイアウト
│   ├── page.tsx           # ホームページ
│   └── globals.css        # グローバルスタイル
├── components/            # Reactコンポーネント
│   └── header.tsx         # ヘッダーコンポーネント（開発用ボタン含む）
├── lib/                   # ユーティリティ
└── public/                # 静的ファイル
```
